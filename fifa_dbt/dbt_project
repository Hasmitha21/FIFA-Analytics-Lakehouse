name: 'fifa_project'
version: '1.0.0'
config-version: 2

profile: 'fifa_project'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fifa_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
